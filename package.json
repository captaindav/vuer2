{
  "name": "vuer",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "node scripts/build.js",
    "clean": "node scripts/clean.sh",
    "lint": "lerna run lint --parallel --stream",
    "postinstall": "lerna bootstrap",
    "serve": "node scripts/serve.js",
    "start": "cd drupal && ddev start"
  },
  "engines": {
    "node": ">= 17"
  },
  "devDependencies": {
    "babel-eslint": "^10.1.0",
    "cross-env": "^7.0.3",
    "cross-spawn": "^7.0.3",
    "eslint": "^8.11.0",
    "lerna": "^4.0.0",
    "lodash": "^4.17.21"
  },
  "dependencies": {
    "jshint": "^2.13.4"
  }
}
